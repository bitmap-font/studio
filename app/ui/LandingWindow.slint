import { ListView, Button } from "std-widgets.slint";
import "../assets/fonts/pretendard/static/Pretendard-Thin.otf";
import "../assets/fonts/pretendard/static/Pretendard-Light.otf";
import "../assets/fonts/pretendard/static/Pretendard-ExtraLight.otf";
import "../assets/fonts/pretendard/static/Pretendard-Regular.otf";
import "../assets/fonts/pretendard/static/Pretendard-Medium.otf";
import "../assets/fonts/pretendard/static/Pretendard-SemiBold.otf";
import "../assets/fonts/pretendard/static/Pretendard-Bold.otf";

export component LandingWindow inherits Window {
    default-font-family: "Pretendard";
    HorizontalLayout {
        padding: 16px;

        VerticalLayout {
            Text {
                text: "@bitmap-font/";

                vertical-stretch: 0;

                font-size: 24px;
            }

            Text {
                text: "STUDIO";

                vertical-stretch: 0;

                font-weight: 900;
                font-size: 16px;
                letter-spacing: 4px;
            }

            Text {
                text: "v0.0.1";

                vertical-stretch: 0;
            }

            Rectangle { }

            HorizontalLayout { }
        }

        VerticalLayout {
            Text {
                text: "Recent Projects";
            }

            ListView { }

            Button {
                text: "(+) New Project";
            }
        }
    }
}
